pluginManagement {
    repositories {
        maven {
            url System.properties['urlMavenRepo']
        }
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
	    maven{
		    url System.properties['urlMavenRepo']
    	}
    }
}

rootProject.name = 'eno'  

include 'eno-core'
include 'ddi-beans'
