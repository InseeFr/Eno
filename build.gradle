allprojects {
    group = 'fr.insee.eno'
    version = '3.0.0-SNAPSHOT'
}

subprojects{
    apply plugin: 'java'
    compileJava {
        options.release = 17
    }
    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
}