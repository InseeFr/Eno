allprojects {
    group = 'fr.insee.eno'
    version = project.properties['version.eno']
}

subprojects{
    apply plugin: 'java'
    compileJava {
        options.release = 17
    }
    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
}